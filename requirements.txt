# Standard public oTree (the example repository used a custom institutional
# fork; nothing in this template depends on it).
otree>=6.0.0

# LLM chat stack of the AI retailer (bot_llm.py): Ollama client + auth.
ollama>=0.4
httpx>=0.27

# Postgres driver for Heroku (oTree reads DATABASE_URL; SQLAlchemy's
# postgres dialect imports psycopg2). Not needed for local sqlite dev.
psycopg2-binary>=2.9
